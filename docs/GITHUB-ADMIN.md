# Админка и GitHub sync

Синхронизация настроена автоматически для `finmanager063-design/regylz`.

- Локально: `.env.local` с `NEXT_PUBLIC_GITHUB_PAT` (не в git).
- Продакшен: секрет репозитория `NEXT_PUBLIC_GITHUB_PAT` в GitHub Actions.

Вход: `/admin`, пароль по умолчанию `1304`.
